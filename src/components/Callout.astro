---
interface Props {
  heading: string;
}

const { heading } = Astro.props;
---

<aside class="not-prose p-6 space-y-0 grid grid-cols-1 gap-2 bg-gray-50 border-l-4 border-l-red-700 shadow-md">
    <h1 class="font-bold text-2xl">{heading}</h1>
    <slot />
</aside>
